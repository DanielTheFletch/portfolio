# Default values
defaults:
  - scope:
      path: {{ baseurl }}
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: {{ baseurl }}
    values:
      layout: "default"

# GitHub Pages compatibility
title: "Jekyll Test"
url: "https://danielthefletch.github.io"
baseurl: "/portfolio"